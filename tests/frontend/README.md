# Frontend tests

Frontend Agent добавляет сюда contract parity, renderer states, keyboard и mobile checks. Начальная машинная проверка fixtures выполняется через `tools/validate_frontend_fixture.py`.
